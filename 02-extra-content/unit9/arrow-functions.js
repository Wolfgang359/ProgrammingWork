var createGreeting = function (message, name) {
    return message + ', ' + name + '!';
};

var createGreetingeES6 = (message, name) => {
    return message + ', ' + name + '!';
}

const createGreetingeES6 = (message, name) => {
    return message + ', ' + name + '!';
}

const createGreetingeES6 = (message, name) => {
    return `${message}, ${name}!`;
}

const createGreetingES6 = (message, name) => `${message}, ${name}!`;


const isEven = num => !(num % 2);

const is3Even = _ => !(3 % 2);



